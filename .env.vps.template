# IMPORTANTE: Questo file contiene variabili sensibili
# NON committare su GitHub! È già in .gitignore

# Flask Secret Key (genera con: openssl rand -hex 32)
SECRET_KEY=cambia-questo-valore-segreto-lungo-e-casuale

# Credenziali Admin
ADMIN_USER=admin
ADMIN_PASSWORD=cambia-questa-password-forte

# File per contatore visite
VIEWS_FILE=/home/deploy/app/port_views

# Playwright browser path
PLAYWRIGHT_BROWSERS_PATH=/home/deploy/app/venv/.playwright-browsers

# AIS Hub API (opzionale per tracking navi)
AISHUB_USERNAME=tuo-username-aishub-se-necessario
